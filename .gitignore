# MCP config (contains API tokens — ne jamais commiter)
.mcp.json

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
ios/
android/

# Metro
.metro-health-check*

# Env (local overrides — .env is committed since it only holds public keys)
.env
.env*.local

# Logs / debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS / IDE
.DS_Store
*.pem
.idea/
.vscode/


# Google Play service account (eas submit) - SECRET, ne jamais committer
play-service-account.json
